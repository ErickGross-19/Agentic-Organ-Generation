# Core dependencies
numpy>=1.20.0
scipy>=1.7.0
trimesh>=3.10.0
networkx>=2.6.0
tqdm>=4.60.0

# Mesh processing
pymeshfix>=0.16.0
scikit-image>=0.18.0

# Optional: NLP optimization (IPOPT solver for better convergence)
# cyipopt is NOT installed by default - the code falls back to scipy if unavailable.
# To install cyipopt:
#   - Windows/Mac: conda install -c conda-forge cyipopt
#   - Linux: pip install cyipopt (requires IPOPT library: apt install coinor-libipopt-dev)
# cyipopt>=1.2.0  # Uncomment if you have IPOPT installed

# Optional: LLM automation
openai>=1.0.0
anthropic>=0.18.0
google-genai>=1.0.0
mistralai>=1.0.0

# Development
pytest>=7.0.0
pytest-cov>=4.0.0
